import { RegisterForm } from '@/components/form/register';
import React from 'react';

const register = () => {
  return (
    <div className='flex items-center justify-center bg-gray-600 h-[100vh]'>
      <RegisterForm />
    </div>
  );
};

export default register;
